/* export function obtenerDatos() {
  const nombre = document.getElementById('nombreRegistro').value;
  console.log(nombre);
  const correo = document.getElementById('c-electronico').value;
  console.log(correo);
  const contraseña = document.getElementsByClassName('contraseña').value;
  console.log(contraseña);

  return (nombre, contraseña, correo);
} const taskForm = document.getElementById('divRegistro');
taskForm.addEventListener('submit', (e) => {
  e.preventDefault();

  const nombre = taskForm['nombreRegistro'].value;
  const correo = taskForm['c-electronico'].value;
  const contraseña = taskForm['submit'].value;
  console.log(nombre, correo, contraseña);
}); */
